#include <iostream>

using namespace std;

int map[100][101] = {};

int main() {
	int m, n;
	cin >> m >> n;
	for (int i = 0; i < m; i++) {
		int temp;
		cin >> ;
	}
	for (int i = 0; i < m; i++) {
		for (int j = 0; j < n; j++) {
			if (map[i][j] == '@') {
				
			}
		}
	}
}
